#!/usr/bin/env bash

# Script to get GitHub activity for a user over a period of time
# Usage: ./summarize-github-activity [username] [start_date] [end_date] [orgs]
# Example: ./summarize-github-activity tomzx 2025-10-20 2025-10-25 "org1,org2" (date range, specific orgs)
# Example: ./summarize-github-activity tomzx 2025-10-20 "" "org1,org2" (from Oct 20 to today, specific orgs)
# Example: ./summarize-github-activity tomzx 2025-10-20 2025-10-25 (date range, all orgs)
# Example: ./summarize-github-activity tomzx 2025-10-20 (from Oct 20 to today, all orgs)
# Example: ./summarize-github-activity (defaults to tomzx, today only, all orgs)
#
# Organizations can also be set via GITHUB_ORGS environment variable (comma-separated)
# Example: GITHUB_ORGS="org1,org2" ./summarize-github-activity tomzx 2025-10-20

# Default values
USER="${1:-tomzx}"
START_DATE="${2:-$(date +%Y-%m-%d)}"
# If end_date is provided, use it; otherwise use today's date
if [ -n "$3" ]; then
    END_DATE="$3"
elif [ -n "$2" ]; then
    # If start_date was provided but end_date wasn't, use today
    END_DATE="$(date +%Y-%m-%d)"
else
    # If neither was provided, use today for both (today only)
    END_DATE="$START_DATE"
fi

# Organizations parameter or environment variable
ORGS="${4:-${GITHUB_ORGS}}"
if [ -n "$ORGS" ]; then
    ORG_DISPLAY=" (Organizations: $ORGS)"
    OWNER_FLAG="--owner=$ORGS"
else
    ORG_DISPLAY=" (All organizations)"
    OWNER_FLAG=""
fi
echo "Fetching GitHub activity for user: $USER$ORG_DISPLAY"
echo "Date range: $START_DATE to $END_DATE"
echo "================================================"
echo ""

# Run all searches in parallel
echo "Fetching data..."
echo ""

# Search for commits (background)
gh search commits --author="$USER" $OWNER_FLAG --author-date="$START_DATE..$END_DATE" --limit 100 --json repository,commit,author,committer --jq '
  group_by(.repository.owner.login + "/" + .repository.name) |
  .[] |
  "  [\(.[0].repository.owner.login)/\(.[0].repository.name)]" + "\n" +
  (map("    - \(.commit.author.date | sub("T"; " ") | sub("Z"; "")) \(.commit.message | split("\n")[0])") | join("\n"))
' > /tmp/gh_commits_$$.txt 2>/dev/null &
PID_COMMITS=$!

# Search for pull requests created (background)
gh search prs --author="$USER" $OWNER_FLAG --created="$START_DATE..$END_DATE" --limit 100 --json repository,title,number,url,state --jq '.[] | "  - [\(.repository.nameWithOwner)#\(.number)] \(.title) (\(.state)) - \(.url)"' > /tmp/gh_prs_$$.txt 2>/dev/null &
PID_PRS=$!

# Search for pull requests reviewed (background)
gh search prs --reviewed-by="$USER" $OWNER_FLAG --updated="$START_DATE..$END_DATE" --limit 100 --json repository,title,number,url,state --jq '.[] | "  - [\(.repository.nameWithOwner)#\(.number)] \(.title) (\(.state)) - \(.url)"' > /tmp/gh_pr_reviews_$$.txt 2>/dev/null &
PID_PR_REVIEWS=$!

# Search for pull requests merged (background)
gh search prs --author="$USER" $OWNER_FLAG --merged-at="$START_DATE..$END_DATE" --limit 100 --json repository,title,number,url,closedAt --jq '.[] | "  - [\(.repository.nameWithOwner)#\(.number)] \(.title) (merged: \(.closedAt | sub("T"; " ") | sub("Z"; ""))) - \(.url)"' > /tmp/gh_prs_merged_$$.txt 2>/dev/null &
PID_PRS_MERGED=$!

# Search for issues created (background)
gh search issues --author="$USER" $OWNER_FLAG --created="$START_DATE..$END_DATE" --limit 100 --json repository,title,number,url,state --jq '.[] | "  - [\(.repository.nameWithOwner)#\(.number)] \(.title) (\(.state)) - \(.url)"' > /tmp/gh_issues_$$.txt 2>/dev/null &
PID_ISSUES=$!

# Search for issues commented on (background)
gh search issues --commenter="$USER" $OWNER_FLAG --updated="$START_DATE..$END_DATE" --limit 100 --json repository,title,number,url,state --jq '.[] | "  - [\(.repository.nameWithOwner)#\(.number)] \(.title) (\(.state)) - \(.url)"' > /tmp/gh_comments_$$.txt 2>/dev/null &
PID_COMMENTS=$!

# Wait for all background jobs to complete
wait $PID_COMMITS
wait $PID_PRS
wait $PID_PR_REVIEWS
wait $PID_PRS_MERGED
wait $PID_ISSUES
wait $PID_COMMENTS

# Display results
echo "📝 Commits:"
COMMITS=$(cat /tmp/gh_commits_$$.txt)
if [ -z "$COMMITS" ]; then
  echo "  None"
else
  echo "$COMMITS"
fi
echo ""

echo "🔀 Pull Requests Created:"
PRS=$(cat /tmp/gh_prs_$$.txt)
if [ -z "$PRS" ]; then
  echo "  None"
else
  echo "$PRS"
fi
echo ""

echo "👀 Pull Requests Reviewed:"
PR_REVIEWS=$(cat /tmp/gh_pr_reviews_$$.txt)
if [ -z "$PR_REVIEWS" ]; then
  echo "  None"
else
  echo "$PR_REVIEWS"
fi
echo ""

echo "✅ Pull Requests Merged:"
PRS_MERGED=$(cat /tmp/gh_prs_merged_$$.txt)
if [ -z "$PRS_MERGED" ]; then
  echo "  None"
else
  echo "$PRS_MERGED"
fi
echo ""

echo "🐛 Issues Created:"
ISSUES=$(cat /tmp/gh_issues_$$.txt)
if [ -z "$ISSUES" ]; then
  echo "  None"
else
  echo "$ISSUES"
fi
echo ""

echo "💬 Issues Commented On:"
COMMENTS=$(cat /tmp/gh_comments_$$.txt)
if [ -z "$COMMENTS" ]; then
  echo "  None"
else
  echo "$COMMENTS"
fi
echo ""

# Clean up temporary files
rm -f /tmp/gh_commits_$$.txt /tmp/gh_prs_$$.txt /tmp/gh_pr_reviews_$$.txt /tmp/gh_prs_merged_$$.txt /tmp/gh_issues_$$.txt /tmp/gh_comments_$$.txt
echo "================================================"
echo "Activity summary complete!"
